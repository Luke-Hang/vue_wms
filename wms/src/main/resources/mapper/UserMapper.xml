<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wms.dao.UserMapper">

    <resultMap type="com.wms.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="no" column="no" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="sex" column="sex" jdbcType="INTEGER"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
        <result property="isvalid" column="isValid" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, no, name, password, age, sex, phone, role_id, isValid
    </sql>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select id, no, name, password, age, sex, phone, role_id, isValid
        from user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
        id, no, name, password, age, sex, phone, role_id, isValid
        from user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="no != null and no != ''">
                and no = #{no}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="sex != null">
                and sex = #{sex}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
            <if test="isvalid != null and isvalid != ''">
                and isValid = #{isvalid}
            </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="no != null and no != ''">
                and no = #{no}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="sex != null">
                and sex = #{sex}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
            <if test="isvalid != null and isvalid != ''">
                and isValid = #{isvalid}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user(no, name, password, age, sex, phone, role_id, isValid)
        values (#{no}, #{name}, #{password}, #{age}, #{sex}, #{phone}, #{roleId}, #{isvalid})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(no, name, password, age, sex, phone, role_id, isValid)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.no}, #{entity.name}, #{entity.password}, #{entity.age}, #{entity.sex}, #{entity.phone},
            #{entity.roleId}, #{entity.isvalid})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(no, name, password, age, sex, phone, role_id, isValid)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.no}, #{entity.name}, #{entity.password}, #{entity.age}, #{entity.sex}, #{entity.phone},
            #{entity.roleId}, #{entity.isvalid})
        </foreach>
        on duplicate key update
        no = values(no),
        name = values(name),
        password = values(password),
        age = values(age),
        sex = values(sex),
        phone = values(phone),
        role_id = values(role_id),
        isValid = values(isValid)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user
        <set>
            <if test="no != null and no != ''">
                no = #{no},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="roleId != null">
                role_id = #{roleId},
            </if>
            <if test="isvalid != null and isvalid != ''">
                isValid = #{isvalid},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from user
        where id = #{id}
    </delete>

    <select id="getUserList" resultMap="UserMap">
        select <include refid="Base_Column_List"></include>
        from user
    </select>

    <select id="findUserByNo" resultMap="UserMap">
        select id, no, name, password, age, sex, phone, role_id, isValid
        from user
        where no = #{userNo}
    </select>

    <select id="queryUser" resultMap="UserMap">
        select <include refid="Base_Column_List"></include>
        from user
        where no = #{no} and password = #{password}
    </select>

</mapper>

